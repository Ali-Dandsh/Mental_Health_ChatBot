now you have a file .env that has the API key change it when the requestes stop 
the second file Practice.Xls this the excel file that contains the practice 
third file explain to you how to run the code and the strcture of FastAPI
number four this the master code and you can run it to check the output on the terminal 
number five this JSON file to store the Conversation history 
number six that the same code of number 4 but with replace the while True with if and try and some proggraming syntax to be run in FastAPI
number seven this the main.py that the file have the fastapi when you run it the API will apper in the terminal 
number eight that select the practice based on th eemation classified by model 
the last file that have the requerments libraries , you should intall it before running the code 
